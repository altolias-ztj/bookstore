<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookstore.mapper.UserMapper">
	<resultMap id="userMap" type="com.bookstore.entity.User">
		<association property="role" select="com.bookstore.mapper.RoleMapper.getByRoleId"
					 column="role_id"/>
	</resultMap>
	<select id="getByUserId" resultMap="userMap">
		select * from users where user_id = #{id}
	</select>
</mapper>